import { describe, it } from "vitest";

export function blockIndentSuite(): void {
    describe("blockIndent", () => {
        it.todo("将 4 空格缩进识别为代码块并转为 fenced code block");
        it.todo("unorderedListIndent 控制无序列表缩进");
        it.todo("orderedListIndent 控制有序列表缩进");
    });
}
